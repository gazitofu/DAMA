# T-04: Revision editing, serialized save and Undo/Redo

Read contract.md, plan T04/AC04–06, latest T02/T03 results and actual Core interfaces/tests. Implement T04 only. Allowed: Packages/DamaCore/ except SSOT-derived DomainContracts.swift and notes/m0-fixture-review/codex/t04-result.md. Preserve other tasks, plan, app/UI/scripts, fixtures and SSOT. No recursive delegation.

This continues the same CLI session: reuse already read unchanged global/domain/rule bodies; remeasure branch/status/current task files and inspect the changed Core API. Create t04-result.md with honest progress immediately and update after implementation/checks. Measured repository interfaces: `FileSessionRepository(rootURL:faultInjector:)`, `loadWithRecovery(sessionId:revisionId:) -> SessionLoadResult` (`document`, `recoveryNotice`), `loadModel(sessionId:runId:)`, `commitRevision(_:)`; fault stages `.afterPartialTemporaryRevisionWrite` / `.afterFinalRevisionInstallation`. Consume these exact existing interfaces. Main-owned later prompts are expected dirty files.

Provide a pure editing operation layer and a stateful editing session suitable for later UI. Reuse codec/validator/repository rather than parallel storage. Supported operations: assign a nonempty set of known selected Word IDs to a known speaker or explicit null; rename exactly one current Run speaker; replace or revert one Word's editedText; split one existing speech Turn at an internal Word boundary; acknowledge/reopen exactly one existing issue. Invalid selections/IDs/empty names/split at first word/marker splitting throw without partial edits. No automatic text rewrite, new transcribed words or speaker inference.

Reassignment changes only selected word speakerId/assignmentSource plus necessary same-current-speaker Turn subdivision. Preserve each preexisting Turn boundary, order and marker; never remerge even adjacent same-speaker Turns. Original word text/prefix/modelSpeakerId/time/alignment/overlap/source IDs/provenance remain. Rename displayName only, never provider labels/other Run. Single editedText carries inheritedUnaligned and 'model original' remains separately accessible; reverting/Undo restores original timing provenance. Split t1 before w2 => five total Turns. Issue acknowledge touches only status, leaves speaker null, marker and Turn.reviewed unchanged.

For explicit text reversion, restore timingOrigin from the immutable model Word or prior snapshot; do not guess .model merely from non-null timestamps. Session initialization can load both current revision and immutable model using the measured repository API.

Editing session exposes committed/current draft state, dirty/saving/failure flags, canUndo/canRedo, and retry/cancel. Serialize apply/save/Undo/Redo/export eligibility across repository suspension; reject or disable overlap while busy. Each successful user operation, Undo or Redo writes a new immutable revision with correct baseRevisionId/sourceRunId/humanEdited/savedAt. Previous snapshots preserve before/after values. Undo/Redo history may be process-local (normal app behavior), but its resulting document/revisions must persist across a new repository/session instance. Do not pretend old Undo history survived if not implemented.

On storage failure, keep dirty intended document and prior committed one with explicit error; never expose canExport or 'saved'. Retry must reuse/resolve the same intended revision safely if an orphan file exists; cancel returns to previous committed content and does not erase files. Only commit success updates committed document/UndoRedo stacks. Tests should inject failures into real temp-root repository, exercise retry/cancel and overlapping commands, and inspect persistent snapshots after reopening.

Provide deterministic issue ordering helper: open issues sorted increasing startUs, ID on ties, null time after timed (not inferred); next issue/last/none behavior explicit. Test i1 .85s before i2 2.1s, tie IDs and null last. UI will consume this helper instead of inventing another sorting policy.

Tests use existing normalized fixture in memory. w5=>C retains '네'/modelB/overlap; rename A leaves B,C and independent Run unchanged; single text edit and Undo/Redo preserve original; t1 split then subsequent speaker edits/UndoRedo/reopen preserve manual boundary (Undo of split itself correctly restores original); acknowledge i1 only leaves i2 open/w5null; empty/invalid operations don't commit. Test no all-speaker cross-Run mapping and no automatic joining across marker.

Run bash scripts/check.sh; do not retry known sandbox denial or run xcodebuild test/signing. Write t04-result.md with actual APIs/state transitions/test results/unverified items. No UI/exporter implementation yet.
