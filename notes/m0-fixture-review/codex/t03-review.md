# T-03 main review follow-up (same task)

Resume the T03 limited delegate after its first implementation. Main read FileSessionRepository.swift completely, codec/validator and fixture. Preserve all existing code/tests/SSOT/fixtures/plan and later main-owned prompts. Allowed changes: FileSessionRepository.swift, its meaningful tests, and t03-result.md. Apply only confirmed review defects below, keep current public protocol signatures. Write report milestones promptly and finish after backpressure.

1. `validateImmutableEvidence` currently compares provenance/speakers/intervals/words/issues, but no marker Turns. Deleting t-marker while retaining separate A speech Turns and unchanged words/issues passes the typed validator and repository. The model's missingSpeech marker ID/time/speaker/wordIds/markerText/issue refs must remain exactly present in M0 edits. Reject added/removed/rewritten marker evidence and cross-model speech-Turn merging, while allowing subdivision and Undo of user-created splits within each original model Turn. Add a regression that removes t-marker without merging the As and expect commit rejection with original active/model/raw hashes intact. Preserve issue acknowledgement behavior.
2. A brand-new import currently calls recoverActive for its expected missing first pointer, recording a recovery notice. First initialization should install its pointer normally with no repair warning. Actual missing/corrupt pointer on an existing stored session still recovers with notice. Test new import zero notices then actual pointer removal and reload with notice.
3. isValidIdentifier permits leading-dot IDs while list/revision scans skip hidden files. Reject such IDs (or consistently enumerate them) before writes, so an accepted import/revision never becomes undiscoverable on restart. Test `.hidden-session` and `.hidden-revision` behavior.
4. `itemExists` uses fileExists, which collapses inaccessible paths into missing. Use a stat/resource check that distinguishes actual ENOENT from permission/I/O failure and reports ioFailure; don't return empty history for unreadable storage. Maintain symlink escape rejection. Save errno immediately after exclusiveLink before cleanup in installImmutable; FileManager cleanup can change it.
5. Recovery already uses Date comparisons. Include the requested differing-offset timestamp regression. Keep main's T02 exact assertions and editedText timing test. Confirm an imported model used to validate revisions cannot itself be a human-edited revision or have a baseRevisionId.

Do not overbuild cross-process locking or power-loss guarantees; current documented limits remain. No new UX work. Run bash scripts/check.sh, classify actual failures and update t03-result.md with this review and final evidence. Never run xcodebuild test/signing or hidden Xcode preference experiments.
