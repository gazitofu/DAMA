# T-03: Immutable session repository

Read contract.md, plan T03/AC07, ssot/design/02_ARCHITECTURE_AND_STORAGE.md §7, T02 result and actual new Core loader/validator/tests. Implement T03 only. Allowed: Packages/DamaCore/ excluding immutable DomainContracts.swift, and notes/m0-fixture-review/codex/t03-result.md. Main owns other prompts and plan. No app/UI/git/SSOT/fixture edits.

Create t03-result.md early with honest in-progress/not-run fields, then update it at implementation and verification milestones. Prior T01/T02 delivered code but stalled before result files; avoid that failure. After required checks finish, update the report and end promptly; do not start cosmetic refactors or further broad testing without a concrete failure. Final chat can be one sentence pointing to the file. Main-created t03.md/t04.md are intentional untracked files.

Main measured T02 interfaces: `NormalizedTranscriptCodec.decode(_ data: Data) throws -> TranscriptDocument` / `encode(_ document: TranscriptDocument) throws -> Data`; `TranscriptValidator().validate(_:)`; `SyntheticTranscriptFixtureLoader.load(data:)` / `load(from:)`; `TranscriptContractError(code:path:)`; `TranscriptTimePresentation.timestamp(_:)`. These exist in TranscriptLoading.swift, TranscriptValidator.swift and TranscriptTimePresentation.swift. Read current definitions and consume them rather than creating another codec/validator.

Implement public actor FileSessionRepository conforming to existing TranscriptRepository unchanged. Use injected root URL for tests and FileManager Application Support /com.gazitofu.Dama for real app construction (do not initialize real app storage in tests). Session paths follow Sessions/<id>/runs/<run>/normalized/model.json, raw fixture source, revisions/<revision>.json and session.json active pointer. Validate path IDs and symlink escape before filesystem access. Read model by explicit sessionId/runId for automatic-version export; list valid existing sessions and expose recovery notices to later UI. Record actual public interfaces.

M0 imports explicit synthetic normalized bytes only; preserve isSynthetic=true and existing engine/provenance. Its stored raw source must be clearly named synthetic input, never falsely labeled a real provider response. Existing separate vendor fixture is not the same Run. Preserve source bytes and immutable normalized model, revisions. Import is idempotent only for an identical source/identity; it must not reset a session's active edits. Conflicting data/IDs must throw. No actual audio/API access.

Commit order: validate document and base/source/session relationships -> new temp revision in same directory -> close/read/validate -> exclusive immutable final revision installation -> active pointer atomically last. Never overwrite a different existing revision, raw or model. Only current active can be base for a new revision; reject stale writes. Safely support retry after interruption leaves an identical unreferenced final revision, without accepting mismatched bytes. When success is uncertain do not claim stored state prematurely. Actor serializes local operations; document any cross-process durability limits.

load(nil) returns valid active. A valid old pointer remains authoritative even if a newer orphan exists after failed commit. Corrupt/missing pointer should recover deterministically to latest valid revision, checking document session/run/identity/validator and ignoring malformed/truncated/temp files. Return a recovery notice via separate internal/public result while keeping protocol load signature. Explicit revision load must never silently substitute another revision. If no valid revision, throw an actionable sanitized error. Do not conceal filesystem failures as empty history. Avoid exposing transcript contents/absolute storage paths in user-facing errors.

Recovery ordering must compare parsed ISO8601 instants (fractional seconds/timezone offsets allowed by existing validator), then a deterministic tie-break; lexicographic savedAt strings are not chronological across offsets. Include a concrete differing-offset case if sorting timestamps is used. Main may add later task prompts while this delegate runs; preserve all of them.

Supported edits must preserve immutable model evidence: original word IDs/text/prefix/modelSpeakerId/times and diarization/provenance remain unchanged for M0 editing; speaker displayName, word speakerId/assignmentSource/editedText/timingOrigin, Turn splitting, issue statuses and new revision metadata are allowed. Use direct base/model comparisons only as needed to protect original evidence; do not forbid valid selection reassignment to a different current speaker. No changing SSOT signatures to fit the implementation.

Fault injection: typed closure/enum stages for partial temp write, final revision installed before pointer, corrupt pointer (test writes corruption to injected root). Tests must inspect raw/model/prior revision byte hashes before/after; new repository instance reload must give prior valid active or explicitly reported recovery. Test failed pointer write retry, malformed candidate, stale base, conflicting existing revision, unsafe IDs, repeat fixture import preserving edits, and separate Runs with identical provider labels remaining independent. Foundation/CryptoKit native standard SDK is fine, no dependencies. Each test owns its unique temp directory and cleanup.

Run check.sh once per code revision. For known sandbox app error, report it and return; do not change scripts or retry sandbox bypasses. Main reruns unsigned compile outside. Write t03-result.md with test results, failure classifications and remaining limitations. T04 editing/Undo/UI not implemented here.
