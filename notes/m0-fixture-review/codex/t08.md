# T-08: End-to-end Core regression and measured developer documentation

Read common contract, plan all AC and T08, actual T04–T07 sources/results, README.md and ssot/dev.md. Implement only remaining concrete M0 integration defects and meaningful journey tests, then update those two developer documents with actual results. Allowed: Packages/DamaCore/ except DomainContracts.swift, App/, UI/, Dama.xcodeproj/, Resources/, README.md, ssot/dev.md, notes/m0-fixture-review/codex/t08-result.md. Scripts only if an actual integration defect demands a narrow fix, keep every cache flag and regression. Preserve fixtures/contracts/design/spec/tone/plan and other reports. Main owns AC/status/commit. Create result progress early. No recursive delegation, app launch, audio/API/Keychain, xcodebuild test, signing/notarization.

Exercise an independent complete synthetic user journey at Core boundaries using real temp-root repository/editor/exporter: import fixture, select/reassign w5, rename A, text edit/Undo/Redo, manual split t1 before w2 with subsequent assignment/Undo/Redo, acknowledge only i1, new repository/editor instance reload, export automatic/current × JSON/TXT. Compare evidence to immutable fixture, selected edit expectations and disk reloaded outputs, not implementation-generated expected snapshots. Assert explicit nulls, original/model evidence, marker order and zero words, current independent boundaries, current provenance, new revision/base lineage and prior hashes. Add only meaningful missing coverage; existing strong tests may be cited instead of duplicated. Failure journey must include dirty save/retry/cancel and export blocking, pointer recovery and prior destination preservation; use measured APIs and actual fault hooks.

Run bash scripts/check.sh once final code/docs are consistent; if an implementation change is needed rerun affected checks then full backpressure. Record actual counts and all4 axes, not stale numbers. Distinguish implementation failures from cache/home/Library/Keychain sandbox refusal. Do not test clean-cache by destructively deleting caches or changing global defaults. Measured history: first T01 fresh Xcode local SwiftPM manifest failed nested sandbox-exec (`sandbox_apply: Operation not permitted`); main reran approved check outside and passed. Later warm-cache inside-sandbox checks passed. Do not turn that into a claim that every clean sandbox build works. No xcodebuild test, code signing or notarization executed.

README must accurately describe implemented M0 synthetic review and current limitations, project/package tree, minimum macOS26.6.2 arm64 (user's M5 MacBook Pro), exact build command and current state linked to versions/m0-fixture-review/plan.md. Native appearance, clicks, quitting/relaunch, keyboard/VoiceOver/light-dark are not measured in this agent session. Do not say M0 built or accuracy proven. Provide concrete external manual steps using real generated app/project paths and actual Debug menu titles. Do not suggest bypassing browser/system security policies.

Update ssot/dev.md old no-project/SKIP passages to actual code. Preserve historical evidence with clear dates/context rather than silently rewrite it. App deployment26.6.2; Core baseline26.0; measured Xcode26.6/Swift6.3.3/SDK26.5 must be distinguished. Fix standalone swiftc example to retain -module-cache-path under .build and use task-specific temp variable, not system HOME; derive exact flags from existing check.sh. Explain Core pure tests vs unsigned Debug compile vs unmeasured native UI/audio/API; original fixture is synthetic hybridPyannoteWhisperKit, no local Whisper or managed processing implemented. Existing spec font manifest has sets:{} and system fonts, do not claim missing-font build failure without evidence.

Result file includes AC01–12 evidence table with full/partial/unmeasured levels and exact source/test references. AC06 UI guards and AC07 UI saved/export behavior cannot be visually certified by Core tests. AC10–12 actual native/VoiceOver journey remains external; explicitly recommend status building with resume to external QA, not built. Main makes final checklist decisions. Include changed files, tests/logs, known limitations, external journey and any remaining genuine defect, without a parallel status document. Finish promptly after bounded verification and report.
