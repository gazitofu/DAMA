# T-06: Native review workspace

Read common contract, plan T06/AC10/12, adopted notes/review-workspace/review-workspace.src.html sections02–04 and ssot/tone/review-workspace.md, actual App/UI/project and T03–T05 APIs/results. Implement T06 only. Allowed: App/, UI/, Dama.xcodeproj/, Resources/, and notes/m0-fixture-review/codex/t06-result.md. Core changes only for a concrete integration defect that you document; never alter DomainContracts/fixtures/SSOT/plan. Main-owned later prompts are intentional. Write result progress early; no native app launch, browser workaround, microphone/API, xcodebuild test, signing/notarization.

Replace ReviewShell with a real SwiftUI/AppKit review workspace connected to the Core repository/editing/export paths. This is native UI, not HTML/WebView, and its actual appearance remains externally unmeasured until main/user runs it. Main read the spec CSS: base1180×760, min900×600, sidebar220, Inspector260; compact width hides Inspector and exposes an actual '근거 보기' popover/overlay with close/escape. Use native system semantic colors/light-dark/focus, readable Korean typography (transcript≈17pt, heading≈21pt, metadata12–13pt), subtle separators, independent dashed/bordered marker row and selection highlight. Don't fake window traffic lights or playback progress.

Main-measured Core APIs: `TranscriptEditingSession.open(repository:sessionId:)`, state/apply/undo/redo/retry/cancelFailedSave; `TranscriptEditingState` exposes committed/draft/isDirty/isSaving/isExporting/failure/canUndo/canRedo/canExport. Export via session.export(selection:format:to:exporter:) using `TranscriptExportSelection(version:sessionId:runId:revisionId:)`, versions `.automatic`/`.current`, formats `.json`/`.text`. This operation holds the editor export lock and validates repository destination before writing; don't bypass it with bare TranscriptExporter.write. Obtain the explicit automatic document using repository.loadModel(sessionId:runId:), current via session.state; preserve IDs. Recovery notices remain in repository.recoveryNotices() after editor open and should be surfaced. Inspect current source for any T05 review changes before using these interfaces.

Bind visible document to explicit automatic/model vs current saved revision. Automatic is read-only and never silently replaced by current edits. Show synthetic provenance badge and honest M0 audio/transcription unsupported status. Session list loads existing valid sessions; errors/recovery notices must surface. Search must have clear/no-results state and not hide active issue navigation silently. No record, no selection, no search match and no open issues must differ; operations disabled appropriately.

Allow keyboard-operable single Word and range/set selection, marker/issue selection, current/model speaker evidence, selected Word speaker reassignment (including explicit unknown), current Run name edit, one Word text edit with original text/timing provenance, split at internal Word boundary, acknowledge/reopen issue, Undo/Redo, retry/cancel dirty save. Reuse Core sorting/edit/save guards. Preserve selection by stable word/issue IDs through edits; recalculate derived Turn selection after splits. Provide visible menus/actions as well as shortcuts; do not rely only on mouse gestures or color. Selection must not trigger nonexistent audio playback.

The explicit one-Word text editor must preserve the user's exact replacement string; disable automatic correction/substitution where supported by the actual native control. Do not trim or normalize transcript text. Speaker display names may be validated for nonempty input using Core rules.

Inspector shows current assignment / model candidate / interval-by-interval confidence map (raw values, nil '미제공') / alignmentScore ratio ('정답 확률이 아님') / overlap warning / human issue status as separate facts. w5 evidence d4 B65,C60 and d5 missing; marker has no words and no fabricated transcription. Issue confirmation never marks speaker resolved or Turn reviewed. Match adopted wording where feature applies; for M0 empty/unsupported use explicit current capability instead of instructing nonexistent recording/import actions.

Saving or failed dirty state disables edit conflicts, version/session switching and export as appropriate. Only actual commit success displays '저장됨'. Retry/cancel are real Core operations. Export uses NSSavePanel with explicit JSON/TXT/current displayed version and normal overwrite confirmation; block app-internal repository destinations before exporter writes. Canceling the panel writes nothing. Do not display internal storage paths/keys/URLs in errors or export. Suggested filename is fine; never save automatically to a chosen directory.

Ensure sources/resources are actually in Xcode target, no missing compile files. Debug fixture entry/menu bar/settings and startup/relaunch orchestration are T07; expose clear initializer or action hooks for them, and do not embed an alternate fake fixture in UI. Empty launch is valid during this task. Accessibility labels for word text/speaker/time/issues/selection, inspector fields and actions; use real controls with keyboard focus and Escape dismissal. Native visual/VoiceOver claims prohibited without actual measurement.

Run bash scripts/check.sh and write exact changed files, measured interfaces, command results/failure classification and what requires real UI verification in t06-result.md. Do not label AC10–12 passed from compile/source inspection.
