# T-02: Contract loading and semantic validation

Read contract.md, T01 result and actual Core sources/package/tests first. Implement plan T02 only. Allowed writes: Packages/DamaCore/ except byte-identical DomainContracts.swift, and this task's t02-result.md. Preserve all other files. Main may have committed T01 and added this prompt; those are intentional.

Inputs already measured: ssot/contracts/DomainContracts.swift and transcript.v1.schema.json, fixtures/normalized-transcript.json and pyannote-job-succeeded.synthetic.json, scripts/SmokeCheck.swift and validate_contracts.py. Read these direct files. Existing normalized:5 words,4 Turns,2 issues, marker850000–990000 with no wordIds, w5 unknown/modelB, d4 map sum125,d5null. Vendor fixture is a separate illustrative DTO, not the same Run transformed to normalized.

Implement a Foundation-only public normalized loader/codec, separate vendor decoder, synthetic-only fixture loader and typed validator. No invented SDK/API calls or P0 normalizer. Make actual interfaces usable by subsequent repository/edit/export/app tasks; record exact signatures in result. Test resources may use deterministic copies of existing fixtures/schema with byte-drift assertions; don't depend on the caller's cwd or change originals.

Validation must cover schemaVersion, IDs/nonempty fields, required nullable JSON key presence (synthesized decodeIfPresent alone allows absent keys), correct types/no unknown normalized object keys, confidence finite0..100 map (sum unrestricted), alignmentScore finite0..1, date/provenance fields per schema, revision sourceRunId match, unique IDs/word ordinals/reference arrays, valid paired-null/nonnegative/nonreversed/in-duration timestamps, interval positive duration, known speaker/interval/issue references, one-and-only-one speech Turn membership per Word, same speaker in speech Turn, marker cannot own words, word order/time containment and existing A/marker/A barrier. No deriving a new merged Turn or editing threshold/algorithmVersion.

Keep nullable times representable; reject partial-null/negative/reversed input. Do not invent times. Unknown vendor status remains a String. Required null keys must survive encode/decode across every nested type; recursively compare against source schema required sets in tests where helpful. Malformed input should throw structured, non-sensitive errors, not crash by building a dictionary with duplicate keys. Synthetic loader must reject provenance.isSynthetic=false rather than flipping it.

Important edit-compatibility review: a user may validly assign w1+w2 from A to C while their model intervals remain A. Do not treat every interval whose speaker differs from the *edited* speaker as a forbidden barrier over the entire span. Validate explicit missing-speech Turn barriers and intervening detected-speaker changes between Words using immutable model evidence. Keep same-current-speaker membership and existing Turn boundaries, without forbidding valid user reassignment. Include a positive test for such a multiword reassignment and a negative for swallowing the B marker into an A Turn. This guards the data validator before T04 consumes it.

Add time presentation helper using Int64 microseconds -> milliseconds ONLY at presentation. Choose and document nearest millisecond with half-up integer rounding, without overflowing on large valid Int64. null/invalid optional inputs present '시간 미확인'; actual invalid documents must still be rejected. Fixed tests850000=>00:00:00.850,990000=>00:00:00.990, difference140000us=>140ms, 849499/849500 rounding, long duration/null; no mutation of model times. Record 3–5 line numeric mini-audit (input/internal/output units, rounding, tests).

Meaningful tests should reject mutations (missing required null key, duplicate ID, dangling ref, negative/reversed time, double/missing membership, invalid marker/mixed Turn, exclusive different-speaker overlap) and exercise valid preserved fixture/unknown status/maps. Run bash scripts/check.sh and write notes/m0-fixture-review/codex/t02-result.md. Never run xcodebuild test, sign or notarize. Do not implement repository, editing or UI yet.
