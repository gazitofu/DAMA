# T-07: Menu bar, Debug fixture and relaunch integration

Read common contract, plan T07/AC11, adopted native review spec/tone, actual T06 App/UI/project sources and T03–T06 result files. Implement T07 only. Allowed: App/, UI/, Dama.xcodeproj/, Resources/, notes/m0-fixture-review/codex/t07-result.md. Core only for a concrete integration defect documented in result. Preserve fixtures/SSOT/plan/scripts/other task files. Create result progress early. No recursive delegation, app launch, audio/API/Keychain, xcodebuild test, signing/notarization.

Integrate the real review workspace with one shared application-owned editing/workspace model. Menu bar icon opens the existing review window; menu also exposes settings and quit. Avoid separate independent writers per window or menu entry. Use actual T06 hooks after reading them; do not replace working review behavior with a parallel shell. Session list/startup reload uses repository persistence. Reopening valid stored current revisions must retain prior edits; surface recovery notices and failures. Empty first launch is truthful and has no automatic fixture import.

Debug-only explicit command `합성 테스트 데이터 열기` imports the existing fixtures/normalized-transcript.json through the synthetic loader/repository. Add the original file as an Xcode resource reference or a deterministic byte-identical derived resource, not a hand-copied alternative fixture. Preserve original source and provenance. The command is compiled out of Release and never auto-executes. Reimport of the same fixture is idempotent and must not replace a user's edited active revision. After import select the actual stored session and show the synthetic badge. Do not add an arbitrary audio/JSON import feature beyond this approved fixture entry.

Main measured original fixture SHA256: `37e41f2055156d2b591cd10ba25165b5d24c1cbb23b2edd41710594d047bfdf8`. After Debug compile, measure bundled resource and compare to actual current original bytes. Do not merely copy this expected hash into a report as measured output.

Settings truthfully explains M0's local synthetic review capability and minimum macOS26.6.2/Apple Silicon. Recording, original-audio playback, transcription, API key entry and cloud upload are not implemented: explicit unsupported text/disabled controls, no fake success, permission request or speculative provider integration. No onboarding that asks for microphone permission in M0.

Guard window close and app quit while a save is pending or failed with dirty intent. Do not silently discard the draft or imply it is saved; provide real retry/cancel-change decisions using the editing session. A clean close should work normally; all actual save results are persisted before presenting saved state. Keep export selection/format/version and internal-destination protection from T05/T06 intact. Keyboard commands for show review/settings/fixture/Undo/Redo must not conflict with editable field standard text Undo unexpectedly. Explain any native responder-chain limitation honestly.

Run bash scripts/check.sh with existing cache flags. Inspect actual target source/resource membership and Debug conditional boundaries. Add meaningful automated integration coverage only where an independently testable non-UI contract needs it; source inspection/Debug compilation is not proof of clicking, relaunching or VoiceOver. Write exact commands/results, resource provenance check, actual launch/menu hooks, save/quit guard behavior, failure classification and external manual journey instructions in t07-result.md. No claim that native UI AC passed without measurement.
