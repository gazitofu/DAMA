# T-01: Native app and Core scaffold

Read notes/m0-fixture-review/codex/contract.md and plan. Implement T-01 only. Allowed writes: Dama.xcodeproj/, App/, UI/, Resources/, Packages/DamaCore/, scripts/check.sh, scripts/check_contract_copy.py (if needed), and notes/m0-fixture-review/codex/t01-result.md. No other edits. Main-owned building/resume and contract/task docs are intentional existing changes.

Create a checked-in Xcode project and shared Dama scheme, macOS26.6.2 arm64 Swift6 app com.gazitofu.Dama, local DamaCore package. Core Foundation module contains a byte-exact derived copy of ssot/contracts/DomainContracts.swift, with a build-time/check.sh drift check. Original absent app/package directories were measured. Do not require an external project generator. Add only a minimal honest app entry/empty review shell for later tasks. Do not auto-load fixture or pretend to record. Add one meaningful module contract decode test if needed to make package test target executable; T02 will add validator/tests.

Update check.sh to run Core pure tests with scratch/cache paths in repo .build and disable SwiftPM sandbox if its nested profile prevents pure test build. Explicitly preserve swiftc -module-cache-path. App axis is compile-only Debug, use CODE_SIGNING_ALLOWED=NO, appropriate module cache/DerivedData/package paths under .build. Do not call xcodebuild test or sign. Report any Xcode/SwiftPM sandbox service/path failures as environment evidence. No network dependency. Keep original Python/Swift smoke checks. Ensure built-in required axis actually compiles the app with its Core dependency, not a placeholder echo.

Run bash scripts/check.sh, fix true code errors, write t01-result.md with actual paths/interfaces/settings/results and measured duration. Do not implement T02+ yet.
